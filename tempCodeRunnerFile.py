print('The original image along with the top-3 predicted segmentation masks and bounding boxes.')
    # mask_data=Dataset(annotation_file=annotation_file,transforms=None)
    # mask_dict=mask_data[Roll_number_last_digit]
    # pred_boxes, pred_masks, pred_classes, pred_scores